spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/y-tanaka0103/springapps-petclinic-microservices-config
        native:
          search-locations: classpath:.
  profiles:
    active: native
